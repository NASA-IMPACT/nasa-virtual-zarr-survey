# discover

Phase 1: enumerate CMR collections into `output/state.json`. Supports
cloud-hosted enumeration, top-N per provider (`usage_score`-ranked), and
top-N total.

::: vzc.discover
    handler: python

# Taxonomy

Empirically-derived error-class classifier. Maps `(error_type, error_message)` tuples to a `Bucket` value using an ordered list of regex rules.

::: nasa_virtual_zarr_survey.taxonomy.Bucket
    handler: python

::: nasa_virtual_zarr_survey.taxonomy.classify
    handler: python
